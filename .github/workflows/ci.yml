name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install uv
        uses: astral-sh/setup-uv@v3
        with:
          version: "latest"
      - name: Set up Python
        run: uv python install 3.11
      - name: Install deps
        run: uv sync --dev
      - name: Ruff lint
        run: uv run ruff check src/ tests/ main.py scripts/
      - name: Ruff format check
        run: uv run ruff format --check src/ tests/ main.py scripts/
      - name: File-size guard (≤150 LOC)
        run: uv run python scripts/check_file_sizes.py
      - name: Pytest with coverage
        run: uv run pytest tests/ --cov=src --cov-report=term-missing --cov-report=xml -v
      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage.xml
          retention-days: 7
