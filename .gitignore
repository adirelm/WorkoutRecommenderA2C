# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
.coverage.*
coverage.xml
htmlcov/

# Virtual env (managed by uv)
.venv/
venv/

# Build artifacts
dist/
build/
*.egg

# IDE / editor
.idea/
.vscode/
*.swp
.DS_Store

# Local environment
.env
.env.local

# A3-specific: instructions/ is local-only (brief PDF, slides, transcript, lecturer template, lecturer feedback)
# Per project rule — must NOT be referenced in shipped docs
instructions/

# Moodle cover sheet — contains PII (ID card + personal name + self-grade); upload to moodle ONLY, never commit
adrl-001-ex*.pdf

# Runtime data — ignored EXCEPT the committed real Kaggle inputs the pipeline
# depends on (full real summary + the §7.2.4 real PHUL subset). The full 294MB
# detailed boostcamp CSV is downloaded on demand and stays ignored.
data/raw/*
!data/raw/program_summary.csv
!data/raw/programs_detailed_phul_subset.csv
data/raw/programs_detailed_boostcamp_kaggle.csv
data/processed/
results/checkpoints/
# results/figures/  # tracked: §7.7 plot deliverables (submission artifacts, not runtime)

# Notebook scratch
.ipynb_checkpoints/

# OS
Thumbs.db

# coverage annotate artifacts
*,cover
